import 'package:flutter/material.dart';

class DiabetologyScreen extends StatelessWidget {
const DiabetologyScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}