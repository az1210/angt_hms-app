import 'package:flutter/material.dart';

class GeneralPracticeScreen extends StatelessWidget {
const GeneralPracticeScreen({ super.key });

  @override
  Widget build(BuildContext context){
    return Container();
  }
}